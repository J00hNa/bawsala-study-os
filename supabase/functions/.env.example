# Required. Exact frontend origins only; comma-separate local and production when needed.
ALLOWED_ORIGINS=http://localhost:8080,https://study.example.com

# Supabase injects SUPABASE_URL, SUPABASE_ANON_KEY, and SUPABASE_SERVICE_ROLE_KEY
# into deployed Edge Functions. Never expose the service-role key in frontend files.
